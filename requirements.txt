# ═══════════════════════════════════════════════
# Jarvis — Phase 1 Dependencies
# ═══════════════════════════════════════════════
#
# We only list what we actually use right now.
# Each phase adds its own dependencies.
#
# Install:   pip install -r requirements.txt
#
# Kokoro (Phase 3) declares Requires-Python <3.13. Use Python 3.10–3.12 for
# this venv (e.g. py -3.10 -m venv venv). Python 3.13+ cannot install kokoro
# from PyPI until the package supports it.
# ═══════════════════════════════════════════════

# Phase 1: No external dependencies needed!
# The skeleton uses only Python standard library:
#   - abc (abstract base classes)
#   - dataclasses
#   - importlib, pkgutil (plugin discovery)
#   - json, os, typing

#Phase 2
requests>=2.32.0               # HTTP client for Ollama API

# Phase 3: Kokoro TTS (text-to-speech)
kokoro>=0.9.4
soundfile>=0.13.0
sounddevice>=0.5.0
numpy>=2.0.0